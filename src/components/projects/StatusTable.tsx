"use client";

// This file is a symlink to the actual implementation in src/components/forms/status-table.tsx
// It exists to maintain backward compatibility with existing imports

import StatusTable, { StatusType } from '@/components/forms/status-table';
export { StatusType };
export default StatusTable; 