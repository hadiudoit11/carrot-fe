"use client";

// This file is a symlink that imports the actual implementation from the forms directory
// It exists to maintain backward compatibility with existing imports

import { default as CreateProjectFormComponent } from '@/components/forms/create-project-form';
export default CreateProjectFormComponent; 